apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: iotp3-playground
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/Tywael/yalthaus-iotp3-application.git"
    targetRevision: master
    path: manifests
  destination:
    server: "https://kubernetes.default.svc"
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
